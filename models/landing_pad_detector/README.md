# Landing pad detector models (planned — Phase 5/8)

Trained weights and export artifacts for landing-pad detection go here
(gitignored if large — track via Git LFS or release assets, not directly in
the repo).

Planned path: OpenCV ArUco marker detection first (no training needed) →
YOLOv8n / MobileNet SSD → ONNX/TensorRT export for Jetson deployment.

See [../../docs/roadmap.md](../../docs/roadmap.md) Phase 5 and Phase 8.
